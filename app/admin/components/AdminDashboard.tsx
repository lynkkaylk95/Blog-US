"use client";
/* eslint-disable @next/next/no-img-element */
import { Fragment, useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { useAdminLocale } from "./AdminLocale";
import { AdminIcon } from "./AdminIcon";

type Post = { id: number; slug: string; title: string; imageUrl: string; status: "draft" | "published"; updatedAt: string; publishedAt: string | null; category: string; categories: string; author: string; views: number; seriesTitle: string | null; partNumber: number | null };
type PostGroup = { key: string; title: string | null; posts: Post[] };
const seriesKey = (value: string) => value.normalize("NFKC").trim().replace(/\s+/g, " ").toLocaleLowerCase("en-US");
const postCategories = (post: Post) => { try { const values = JSON.parse(post.categories) as string[]; return values.length ? values : [post.category]; } catch { return [post.category]; } };

export function AdminDashboard() {
  const { locale, t } = useAdminLocale();
  const [posts, setPosts] = useState<Post[]>([]); const [query, setQuery] = useState("");
  const [filter, setFilter] = useState("all"); const [authorFilter, setAuthorFilter] = useState("all"); const [categoryFilter, setCategoryFilter] = useState("all");
  const [dateFrom, setDateFrom] = useState(""); const [dateTo, setDateTo] = useState(""); const [viewsFrom, setViewsFrom] = useState(""); const [viewsTo, setViewsTo] = useState("");
  const [perPage, setPerPage] = useState(10); const [page, setPage] = useState(1); const [expanded, setExpanded] = useState<Set<string>>(new Set()); const [message, setMessage] = useState("");
  async function load() { const response = await fetch("/api/admin/posts"); const result = await response.json() as { posts?: Post[]; message?: string }; if (response.status === 401) return void (window.location.href = "/admin/login"); setPosts(result.posts || []); setMessage(response.ok ? "" : result.message || t("couldNotLoad")); }
  // eslint-disable-next-line react-hooks/exhaustive-deps
  useEffect(() => { const timer = window.setTimeout(() => void load(), 0); return () => window.clearTimeout(timer); }, []);
  const authors = useMemo(() => [...new Set(posts.map((post) => post.author).filter(Boolean))].sort(), [posts]);
  const categories = useMemo(() => [...new Set(posts.map((post) => post.category).filter(Boolean))].sort(), [posts]);
  const visible = useMemo(() => posts.filter((post) => { const date = post.publishedAt?.slice(0, 10) || ""; const min = viewsFrom === "" ? null : Number(viewsFrom); const max = viewsTo === "" ? null : Number(viewsTo); return (filter === "all" || post.status === filter) && (authorFilter === "all" || post.author === authorFilter) && (categoryFilter === "all" || post.category === categoryFilter) && (!dateFrom || (date && date >= dateFrom)) && (!dateTo || (date && date <= dateTo)) && (min === null || post.views >= min) && (max === null || post.views <= max) && `${post.title} ${post.slug} ${post.seriesTitle || ""}`.toLowerCase().includes(query.trim().toLowerCase()); }), [posts, query, filter, authorFilter, categoryFilter, dateFrom, dateTo, viewsFrom, viewsTo]);
  const groups = useMemo(() => { const result: PostGroup[] = []; const series = new Map<string, PostGroup>(); for (const post of visible) { if (!post.seriesTitle) { result.push({ key: `post-${post.id}`, title: null, posts: [post] }); continue; } const key = `series-${seriesKey(post.seriesTitle)}`; let group = series.get(key); if (!group) { group = { key, title: post.seriesTitle, posts: [] }; series.set(key, group); result.push(group); } group.posts.push(post); } result.forEach((group) => group.posts.sort((a, b) => (a.partNumber || 0) - (b.partNumber || 0))); return result; }, [visible]);
  const pageCount = Math.max(1, Math.ceil(groups.length / perPage)); const currentPage = Math.min(page, pageCount); const paginated = groups.slice((currentPage - 1) * perPage, currentPage * perPage); const published = posts.filter((post) => post.status === "published").length;
  function resetFilters() { setQuery(""); setFilter("all"); setAuthorFilter("all"); setCategoryFilter("all"); setDateFrom(""); setDateTo(""); setViewsFrom(""); setViewsTo(""); setPage(1); }
  function toggle(key: string) { setExpanded((current) => { const next = new Set(current); if (next.has(key)) next.delete(key); else next.add(key); return next; }); }
  function insertPartUrl(post: Post) { return `/admin/series/new?${new URLSearchParams({ seriesTitle: post.seriesTitle || "", partNumber: String(post.partNumber || 1), categories: JSON.stringify(postCategories(post)), author: post.author, imageUrl: post.imageUrl })}`; }
  async function remove(post: Post) { if (!confirm(t("deleteConfirm", { title: post.title }))) return; const response = await fetch(`/api/admin/posts/${post.id}`, { method: "DELETE" }); if (response.ok) setPosts((items) => items.filter((item) => item.id !== post.id)); else alert(t("deleteFailed")); }
  async function copy(post: Post) { await navigator.clipboard.writeText(`${window.location.origin}/story/${post.slug}`); setMessage(t("linkCopied")); window.setTimeout(() => setMessage(""), 1800); }
  async function importStories() { setMessage(t("importing")); const response = await fetch("/api/admin/import-markdown", { method: "POST" }); const result = await response.json() as { imported?: number; message?: string }; if (!response.ok) return setMessage(result.message || t("importFailed")); setMessage(t("imported", { count: result.imported || 0 })); await load(); }
  const actions = (post: Post) => <div className="admin-actions">{post.status === "published" && <Link title={t("view")} href={`/story/${post.slug}`} target="_blank"><AdminIcon name="eye" /></Link>}<button title={t("copyLink")} onClick={() => copy(post)}><AdminIcon name="copy" /></button>{post.seriesTitle && <Link title={`Insert before Part ${post.partNumber || 1}`} href={insertPartUrl(post)}><AdminIcon name="plus" /></Link>}<Link title={t("edit")} href={`/admin/posts/${post.id}`}><AdminIcon name="edit" /></Link><button className="danger" title={t("delete")} onClick={() => remove(post)}><AdminIcon name="trash" /></button></div>;
  const cells = (post: Post, child = false) => <><td><div className={`admin-post-title${child ? " admin-post-title--part" : ""}`}><img src={post.imageUrl} alt="" /><div><b>{child ? `Part ${post.partNumber || "–"}: ${post.title}` : post.title}</b><small>/{post.slug}</small></div></div></td><td>{post.author}</td><td>{post.views.toLocaleString(locale === "vi" ? "vi-VN" : "en-US")}</td><td>{post.category}</td><td>{new Date(post.updatedAt).toLocaleDateString(locale === "vi" ? "vi-VN" : "en-US")}</td><td><span className={`status status--${post.status}`}>{t(post.status)}</span></td><td>{actions(post)}</td></>;
  return <main className="admin-main"><div className="admin-page-title"><div><span>{t("blog")}</span><h1>{t("managePosts")}</h1><p>{t("managePostsDescription")}</p></div><div className="admin-title-actions"><button type="button" onClick={importStories}><AdminIcon name="upload" />{t("importStories")}</button><Link className="admin-primary" href="/admin/posts/new"><AdminIcon name="plus" />{t("addPost")}</Link></div></div><section className="admin-stats"><article><span>{t("totalPosts")}</span><b>{posts.length}</b></article><article><span>{t("published")}</span><b>{published}</b></article><article><span>{t("drafts")}</span><b>{posts.length - published}</b></article></section><section className="admin-panel"><div className="admin-filter-panel"><div className="admin-filters"><label><span>{t("search")}</span><input type="search" placeholder={t("searchPlaceholder")} value={query} onChange={(e) => { setQuery(e.target.value); setPage(1); }} /></label><label><span>{t("status")}</span><select value={filter} onChange={(e) => { setFilter(e.target.value); setPage(1); }}><option value="all">{t("allStatuses")}</option><option value="published">{t("published")}</option><option value="draft">{t("draft")}</option></select></label><label><span>{t("author")}</span><select value={authorFilter} onChange={(e) => { setAuthorFilter(e.target.value); setPage(1); }}><option value="all">{t("allAuthors")}</option>{authors.map((value) => <option key={value}>{value}</option>)}</select></label><label><span>{t("category")}</span><select value={categoryFilter} onChange={(e) => { setCategoryFilter(e.target.value); setPage(1); }}><option value="all">{t("allCategories")}</option>{categories.map((value) => <option key={value}>{value}</option>)}</select></label><label><span>{t("publishedFrom")}</span><input type="date" value={dateFrom} onChange={(e) => { setDateFrom(e.target.value); setPage(1); }} /></label><label><span>{t("publishedTo")}</span><input type="date" value={dateTo} min={dateFrom || undefined} onChange={(e) => { setDateTo(e.target.value); setPage(1); }} /></label><label><span>{t("viewsFrom")}</span><input type="number" min="0" value={viewsFrom} onChange={(e) => { setViewsFrom(e.target.value); setPage(1); }} /></label><label><span>{t("viewsTo")}</span><input type="number" min="0" value={viewsTo} onChange={(e) => { setViewsTo(e.target.value); setPage(1); }} /></label></div><div className="admin-filter-footer"><button type="button" className="admin-reset-filters" onClick={resetFilters}>{t("resetFilters")}</button><label className="admin-page-size"><span>{t("show")}</span><select value={perPage} onChange={(e) => { setPerPage(Number(e.target.value)); setPage(1); }}>{[10,20,50,100].map((value) => <option key={value}>{value}</option>)}</select><span>{t("perPage")}</span></label><span>{groups.length} {t("posts").toLowerCase()}</span></div></div>{message && <div className="admin-notice" role="status">{message}</div>}<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>{t("story")}</th><th>{t("author")}</th><th>{t("views")}</th><th>{t("category")}</th><th>{t("updated")}</th><th>{t("status")}</th><th>{t("actions")}</th></tr></thead><tbody>{paginated.map((group) => { if (!group.title) return <tr key={group.key}>{cells(group.posts[0])}</tr>; const open = expanded.has(group.key); const lead = group.posts[0]; const allPublished = group.posts.every((post) => post.status === "published"); return <Fragment key={group.key}><tr className="admin-series-row"><td><button className="admin-series-toggle" type="button" onClick={() => toggle(group.key)} aria-expanded={open}><span className="admin-series-chevron">›</span><img src={lead.imageUrl} alt="" /><span><b>{group.title}</b><small>{group.posts.length} Parts</small></span></button></td><td>{lead.author}</td><td>{group.posts.reduce((sum, post) => sum + post.views, 0).toLocaleString(locale === "vi" ? "vi-VN" : "en-US")}</td><td>Series</td><td>{new Date(Math.max(...group.posts.map((post) => Date.parse(post.updatedAt)))).toLocaleDateString(locale === "vi" ? "vi-VN" : "en-US")}</td><td><span className={`status status--${allPublished ? "published" : "draft"}`}>{allPublished ? t("published") : t("draft")}</span></td><td><button className="admin-expand-button" type="button" onClick={() => toggle(group.key)}>{open ? "Hide" : "Show"} {group.posts.length} Parts</button></td></tr>{open && group.posts.map((post) => <tr className="admin-series-part-row" key={post.id}>{cells(post, true)}</tr>)}</Fragment>; })}</tbody></table>{!message && !groups.length && <div className="admin-empty">{t("noPosts")}</div>}</div>{groups.length > 0 && <nav className="admin-pagination" aria-label={t("pagination")}><button type="button" disabled={currentPage === 1} onClick={() => setPage(currentPage - 1)}>{t("previous")}</button>{Array.from({ length: pageCount }, (_, i) => i + 1).map((number) => <button type="button" key={number} className={number === currentPage ? "active" : ""} onClick={() => setPage(number)}>{number}</button>)}<button type="button" disabled={currentPage === pageCount} onClick={() => setPage(currentPage + 1)}>{t("next")}</button></nav>}</section></main>;
}
